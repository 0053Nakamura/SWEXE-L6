<% if flash[:notice] %>
    <%= flash[:notice] %>
<% end %>

商品ページ
<% @products.each do |e| %>
    <p>
        <%= e.name %>
        <%= e.price %>円
        <%= link_to '削除', "/products/#{e.id}", method: :delete %>
        <%= link_to 'カートに追加', new_cartitem_path(product_id: e.id) %>
    </p>
<% end %>

<p>
    商品の登録は<%= link_to 'こちら', new_product_path %><br>
    カートの中身は<%= link_to 'こちら', cart_path(current_cart.id) %>
</p>